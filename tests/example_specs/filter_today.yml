# "today"/"today_end" resolve to actual dates at build time (see
# cimon.workflows.query_spec), so this never needs manual edits.
all:
  - column: created_at
    op: between
    value: ["today", "today_end"]
